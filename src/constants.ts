export const COLLECTIONS = {
  USER: 'USER',
  BUCKET: 'BUCKET',
}

export const REGEX = {
  NICKNAME: /^[ㄱ-ㅎㅏ-ㅣ가-힣a-zA-Z0-9]*$/,
  EMAIL: /^[\w.-]+@[a-zA-Z\d.-]+\.[a-zA-Z]{2,}$/,
  PASSWORD: /^.{8,}$/,
}
